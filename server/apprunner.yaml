version: 1.0
runtime: nodejs18

build:
  commands:
    build:
      - npm ci
      - npm run build
      - npx prisma generate
      - npx prisma db push

run:
  runtime-version: 18
  command: node dist/index.js
  network:
    port: 8080
  env:
    - name: DATABASE_URL
      value: file:./dev.db
